node_modules
.next
out
*.log
.DS_Store
next-env.d.ts
tsconfig.tsbuildinfo

# secrets / local config — never commit (keep only .env.example)
.env*
!.env.example
.vercel*

# csdx import backups + logs (large / may contain content dumps)
_backup_*
logs/
contentstack-cli-logs/
*.bak
